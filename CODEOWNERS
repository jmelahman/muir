# The default-branch ruleset (see terraform/) requires a review from the owner
# below on EVERY pull request — so all contributions go through you. The muir
# bot App is a ruleset bypass actor, so its audit-clean update PRs auto-merge
# without this review; everyone else's PRs wait for your approval.
#
# bootstrap.sh rewrites this to your fork's owner.
* @jmelahman
